import React from "react";
import { makeStyles } from "@mui/styles";
const useStyles = makeStyles((theme) => {
  return {
    root: {
      
    },
  };
});

const DemoGames = () => {
  const classes = useStyles();
  return <div>DemoGames</div>;
};

export default DemoGames;
